{
  "name": "qubes-covenant",
  "version": "1.0.0",
  "private": true,
  "description": "CashScript covenant for permissionless Qube NFT minting",
  "type": "module",
  "scripts": {
    "compile": "cashc qubes_mint.cash --output qubes_mint.json",
    "compile:wallet": "cashc qubes_wallet.cash --output qubes_wallet.json",
    "deploy": "tsx deploy.ts",
    "migrate": "tsx migrate.ts",
    "mint": "tsx mint-cli.ts",
    "wallet": "tsx wallet-cli.ts"
  },
  "dependencies": {
    "@bitauth/libauth": "^3.1.0-next.8",
    "cashscript": "^0.12.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
