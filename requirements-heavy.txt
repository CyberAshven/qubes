# Requirements for Heavy Bundle (includes ALL AI features)
# This is requirements-ci.txt + torch, kokoro, sentence-transformers, etc.
# Used by scripts/build-heavy-linux.sh and .github/workflows/build.yml
#
# For CUDA torch (GPU acceleration), install with:
#   pip install torch torchaudio --extra-index-url https://download.pytorch.org/whl/cu124
# CI uses CPU-only torch to keep bundle under 2GB GitHub release limit.

torch>=2.2.0
torchaudio>=2.2.0

# Core
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
colorama>=0.4.6

# Cryptography & Blockchain
cryptography>=41.0.0
ecdsa>=0.18.0
bitcash>=0.6.0
base58>=2.1.0

# Games
chess>=1.9.0

# Logging
structlog>=24.1.0

# Error handling
tenacity>=8.2.0

# System
psutil>=5.9.0

# AI providers
openai>=1.0.0
anthropic>=0.8.0
httpx>=0.27.0

# Networking
aiohttp>=3.8.0
requests>=2.31.0
websockets>=12.0

# CLI
typer>=0.9.0
rich>=13.0.0

# Web
beautifulsoup4>=4.12.0

# Audio/TTS
sounddevice>=0.4.6
numpy>=1.24.0
pydub>=0.25.1
soundfile>=0.12.0
pyaudio>=0.2.13
elevenlabs>=1.0.0
google-cloud-texttospeech>=2.14.0
huggingface_hub>=0.23.0

# Document processing
Pillow>=10.0.0
PyMuPDF>=1.23.0

# Google AI
google-genai>=1.0.0

# Timezone
pytz>=2023.3

# Server
fastapi>=0.100.0
uvicorn>=0.23.0

# Monitoring & Resilience
prometheus_client>=0.17.0
pybreaker>=1.0.0

# === HEAVY-ONLY PACKAGES BELOW ===

# Kokoro TTS (fast lightweight local TTS - 82M params, 54 voices)
kokoro>=0.9.4
misaki[en,ja,zh,ko]>=0.9.4
unidic-lite>=1.0.8

# Semantic search
sentence-transformers>=3.3.1
faiss-cpu>=1.9.0

# Transformers ecosystem (needed by kokoro, sentence-transformers)
transformers>=4.40.0
accelerate>=0.25.0
safetensors>=0.4.0
tokenizers>=0.15.0

# Qwen3-TTS (voice design + cloning; models download on demand via UI)
qwen-tts>=0.0.5

# PyInstaller (build tool)
pyinstaller>=6.0.0
